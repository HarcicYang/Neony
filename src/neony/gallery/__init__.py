"""Neony gallery — the component showcase app.

``neony.gallery`` is an opt-in subpackage: importing it constructs the
:class:`~neony.application.NeonApplication` instance (side effect), so
``neony/__init__.py`` deliberately does not import it.  Launch with
``uv run gallery`` or ``python -m neony.gallery``.
"""
